name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Diagnose
        shell: bash
        run: |
          BINS=$(ls node_modules/.bin 2>/dev/null | tr '\n' ',')
          EV=$(test -e node_modules/electron-vite/bin/electron-vite.js && echo YES || echo NO)
          PKG=$(test -d node_modules/electron-vite && echo YES || echo NO)
          echo "::error::node=$(node -v) NODE_ENV=[$NODE_ENV] ev_pkg=$PKG ev_bin=$EV"
          echo "::error::BINS=$BINS"

      - name: Build
        run: npm run build

      - name: Package and publish
        run: npm run release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
